extends ../base

block content
    form(method="POST" enctype="multipart/form-data")
        input(type="file" name="bgImage" accept="image/*" required)
        input(type="submit" value="Upload New Background Image")