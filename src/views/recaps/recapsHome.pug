extends ../base   

block content
    form(method="GET" action="/recaps/search")
        input(type="search" name="keyword" placeholder="Search by title")
        input(type="submit" value="Find Recap")
    a(href="/recaps/create") Create Recaps!